# ============================================================
# 行尾契约（首次公开提交即生效，之后不再变更）
# ============================================================
# 仓库统一 LF。Windows 工作区不保留混合换行：
# 检出时由 Git 按 eol=lf 归一，提交时同样归一，
# 避免「本地改了行尾、diff 全是整文件重写」这类噪音。
* text=auto eol=lf

# ============================================================
# 二进制文件：不要做文本归一化，否则 Git 会尝试转换并破坏内容
# ============================================================
*.png  -text
*.jpg  -text
*.jpeg -text
*.gif  -text
*.ico  -text
*.webp -text

# 备注：本仓库暂无必须使用 CRLF 的 .bat / .cmd 脚本。
# 若日后新增，单独补 `*.bat text eol=crlf` 即可，不要改动上面的全局规则。
